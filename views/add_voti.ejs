<%- include layouts/header.ejs %>

        <div class="page-data">

            <form method="post" action="/voti/add/add">
                <table cellpadding="11">
                <tr>
                    <td class="label">Voto</td><td>: <input type="text" name="voto" required></td>
                 </tr>
				 <tr>
                    <td class="label">
                      Nome</td>
                    <td>
                      <select name="id" required>

                    <% if(data.length){

                         for(var i = 0;i < data.length;i++) { %>
                            <option value="<%=data[i].id%>"><%=data[i].cognome%></option>
                         <% }

                     }else{ %>
                       <tr>
                          <option value="null">No studenti</option>
                       </tr>
                    <% } %>
                      </select>
                    </td>
                 </tr>
				 <tr>
                    <td class="label"></td>
                    <td>
                        &nbsp; <input type="submit" name="submit" value="Save">
                       <input type="button" name="cancel" value="Cancel" class="cancel" onClick="cancelAdd()">
                    </td>
                 </tr>
                </table>
            </form>
        </div>

<%- include layouts/footer.ejs %>
